# ── 数据库 ──
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=hotel123456
DB_NAME=hotel_db

# ── Redis ──
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# ── JWT ──
JWT_SECRET_KEY=change-me-to-a-random-64-char-string-in-production
JWT_EXPIRE_HOURS=24

# ── 运行环境 ──
APP_ENV=development
SERVER_PORT=8080

# ── 短信验证码 ──
SMS_PROVIDER=mock
SMS_MOCK_CODE=888888

# ── 支付 ──
PAYMENT_MODE=mock
